
let board = Array(10).fill(Array(10).fill(false));
function checkCell (i, j) {
  let liveNeighbors = 0;
  if (i > 0) {
    board[]
  }
}

function checkIndex (i, j) {
  if (i >= 0 && j >= 0 && i <= 9 && j <= 9) {
    return true;
  } else {
    return false;
  }
}
